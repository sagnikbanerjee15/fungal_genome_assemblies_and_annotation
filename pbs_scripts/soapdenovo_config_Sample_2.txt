# maximal read length
max_rd_len=300

[LIB]
# average insert size
avg_ins=566

# if sequence needs to be
reversed reverse_seq=0

# in which part(s) the reads are used
asm_flags=3

# use only first 300 bps of each read
rd_len_cutoff=300

# in which order the reads are used while scaffolding
rank=1

# cutoff of pair number for a reliable connection (default 3)
pair_num_cutoff=3

# minimum aligned length to contigs for a reliable read location (default 32)
map_len=100

# fastq file for read 1
q1=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/2_S2_L001_cov_paired_trim_R1_001.fastq

# fastq file for read 2 always follows fastq file for read 1
q2=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/2_S2_L001_cov_paired_trim_R2_001.fastq

[LIB]
# average insert size
avg_ins=2551

# if sequence needs to be
reversed reverse_seq=0

# in which part(s) the reads are used
asm_flags=3

# use only first 300 bps of each read
rd_len_cutoff=300

# in which order the reads are used while scaffolding
rank=1

# cutoff of pair number for a reliable connection (default 3)
pair_num_cutoff=3

# minimum aligned length to contigs for a reliable read location (default 32)
map_len=100

# fastq file for read 1
q1=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/2_S2_L001_enz_paired_trim_R1_001.fastq

# fastq file for read 2 always follows fastq file for read 1
q2=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/2_S2_L001_enz_paired_trim_R2_001.fastq

[LIB]
# average insert size
avg_ins=466

# if sequence needs to be
reversed reverse_seq=0

# in which part(s) the reads are used
asm_flags=3

# use only first 300 bps of each read
rd_len_cutoff=300

# in which order the reads are used while scaffolding
rank=1

# cutoff of pair number for a reliable connection (default 3)
pair_num_cutoff=3

# minimum aligned length to contigs for a reliable read location (default 32)
map_len=75

# fastq file for read 1
q1=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/65_S65_paired_trim_R1.fastq

# fastq file for read 2 always follows fastq file for read 1
q2=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/65_S65_paired_trim_R2.fastq

[LIB]
# average insert size
avg_ins=554

# if sequence needs to be
reversed reverse_seq=0

# in which part(s) the reads are used
asm_flags=3

# use only first 300 bps of each read
rd_len_cutoff=300

# in which order the reads are used while scaffolding
rank=1

# cutoff of pair number for a reliable connection (default 3)
pair_num_cutoff=3

# minimum aligned length to contigs for a reliable read location (default 32)
map_len=75

# fastq file for read 1
q1=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/68_S68_paired_trim_R1.fastq

# fastq file for read 2 always follows fastq file for read 1
q2=/work/LAS/rpwise-lab/sagnik/3FG/data/Sample_2/68_S68_paired_trim_R2.fastq
